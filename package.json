{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --silent --noStackTrace",
    "test:unit": "npm test -- --watch -c jest-unit-config.js",
    "test:integration": "npm test -- --watch -c jest-integration-config.js",
    "test:ci": "npm test -- --watchAll --coverage -c jest-integration-config.js",
    "test:staged": "jest --passWithNoTests"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "author": "Renato Ramos",
  "license": "ISC",
  "devDependencies": {
    "husky": "^4.0.0-beta.5",
    "jest": "^26.6.3",
    "lint-staged": "^10.5.3",
    "standard": "^16.0.3"
  },
  "dependencies": {}
}
